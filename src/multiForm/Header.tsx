import React from "react";
import styled from "styled-components";

const Header = () => {
  return <Container>No Lazy Loading Applied</Container>;
};

export default Header;
const Container = styled.h1`
  padding: 0 10px;
`;
